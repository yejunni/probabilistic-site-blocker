# 확률 기반 사이트 차단 확장 프로그램

## 프로젝트 개요
고등학교 정보과학 수업 프로젝트 (8차시).
차단된 사이트에 접속하면 확률로 통과 여부를 판정하고,
통과 시 하루 한도 안에서 고른 시간만큼만 접속을 허용한다.

만드는 것:
1. **크롬 확장 프로그램** (메인) — 차단, 판정, 타이머 ← 지금 여기
2. **웹 대시보드** — 기록 통계, Vercel 배포, Supabase 연동 (6차시부터)

일정: 1~5차시 프론트, 6~8차시 Supabase

---

## 작업 방식 (제일 중요)
- **나는 웹 개발 초보다.** 코드를 한 번에 다 만들지 말고 작은 단위로
  나눠서 진행하고, 각 코드가 무슨 역할인지 설명해줘.
- 내가 이해하고 **직접 수정할 수 있는 수준**을 목표로 한다.
  변수명은 알아보기 쉽게, 주석은 "왜 이렇게 했는지" 위주로.
- 파일을 새로 만들거나 크게 고치기 전에 **먼저 물어보고 설명해줘.**
- 화면 꾸미기(CSS)는 나중에 한 번에 할 거라 지금은 최소한으로만.

---

## 실행 / 테스트 방법

빌드 과정 없음. 순수 HTML/CSS/JS라 파일을 그대로 크롬에 올린다.

1. `chrome://extensions` → 개발자 모드 켜기 → "압축해제된 확장 프로그램을 로드"
2. **코드를 고친 뒤에는 반드시 카드의 새로고침(↻) 버튼을 누를 것.**
   이걸 빼먹으면 바뀐 코드가 반영되지 않는다.
3. `youtube.com` 접속 → 차단 화면이 뜨면 정상

**오류 확인**: `chrome://extensions`에서 이 확장의 **"서비스 워커"** 링크를 누르면
`background.js` 전용 콘솔이 열린다. 여기에 오류가 찍힌다.

**시간 건너뛰기**: 80분 확률을 보려고 80분을 기다릴 수는 없으므로,
차단 화면 맨 아래 테스트 칸에서 경과 시간과 오늘 사용량을 강제로 설정할 수 있다.
(`DEV_SET_ELAPSED` / `DEV_SET_USED`)

이 칸은 `config.js`의 **`devMode`** 로 켜고 끈다. 제출할 때 `false`로 바꾸면
칸이 사라지고 요청 자체도 무시된다. **코드를 지울 필요 없다.**

---

## 전체 흐름

```
유튜브 접속
   ↓  rules.json (정적 규칙, priority 1) 이 blocked.html로 리다이렉트
차단 화면 — 확률 표시, 1초마다 갱신
   ↓  [시도하기]
background.js가 난수(0~100)를 뽑아 확률과 비교
   ├─ 주사위 < 확률 → 통과
   │      ↓  5/10/20/30분 중 선택
   │   priority 2짜리 allow 동적 규칙을 덮어씌워 차단 해제
   │   사용량을 미리 차감 + chrome.alarms 예약
   │      ↓  유튜브로 이동. timer.js가 우측 상단에 남은 시간 표시
   │   알람 발동 → allow 규칙 제거 → 열린 유튜브 탭 강제 새로고침
   │      ↓
   │   다시 차단 화면. 이 시각이 새 기준 (확률 0%부터 다시)
   │
   └─ 주사위 ≥ 확률 → 거부
          쿨타임 60초 + 경과 시간도 0으로 초기화
```

---

## 파일 구조

| 파일 | 역할 |
|---|---|
| `manifest.json` | 확장 설정. 권한, background, content_scripts, 규칙 경로 |
| `rules.json` | 정적 차단 규칙. youtube.com(main_frame) → blocked.html |
| `config.js` | **확률·시간 관련 모든 숫자.** 조정은 여기서만 |
| `probability.js` | `calcProb(경과분, 오늘사용분)` → 확률(%). 계산만 한다 |
| `background.js` | 서비스 워커. 판정, 차단 해제/재차단, 알람, storage 저장 |
| `blocked.html` | 차단 화면 뼈대 (꾸미기는 미뤄둔 상태) |
| `blocked.js` | 차단 화면 버튼 동작. 계산은 안 하고 background에 물어본다 |
| `timer.js` | content script. 유튜브 페이지 위에 남은 시간을 띄운다 |

### 역할 분리 원칙
- `probability.js` — **순수 계산만.** 저장·화면·난수 전부 금지.
  따로 떼어내서 검산할 수 있어야 한다
- `background.js` — 상태와 판정의 **유일한 주인.** 난수도 여기서만 뽑는다
- `blocked.js` / `timer.js` — **묻고 그리기만.** 확률을 직접 계산하지 않는다

`config.js`와 `probability.js`는 `importScripts`로 서비스 워커에 불려간다.
그래서 ES 모듈(`export`)이 아니라 **전역 스크립트**로 작성돼 있다.
(서비스 워커가 classic이라 `manifest.json`에 `"type": "module"`이 없다)

---

## 확률 수식

전부 `config.js`에 있다. **다른 파일에는 숫자를 쓰지 않는다.**
아래는 조정하면서 쓰는 시작값이고 확정된 값이 아니다.

기본 개형은 **scurve**(로지스틱). `curve` 값으로 셋 중 하나를 고른다.

**scurve** — `midpoint: 60`, `steepness: 0.05`
```
s(t) = 1 / (1 + exp(-steepness * (t - midpoint)))
p    = 100 * (s(t) - s(0)) / (1 - s(0))
```
> `s(0)`을 빼는 이유: 그냥 쓰면 **0분에서도 4.7%가 나온다.**
> 기다리지도 않았는데 확률이 생기면 안 되므로 정규화한다.

**log** — `smoothness: 30`
```
p = A * ln(1 + t / smoothness)
```
> A는 고정값이 아니라 "`anchorMinutes`(80분)에 `anchorProb`(50%)"에서 **역산**한다.
> 그래서 anchor 값만 바꾸면 곡선 전체가 따라온다.

**linear** — `linearFullMin: 150`
```
p = maxProb * (t / linearFullMin)
```

### 공통 보정
- `maxProb: 85` — 상한. 세 개형 모두 여기서 자른다
- `dailyLimit: 60` — 하루 총 이용 한도(분)
- `durationOptions: [5, 10, 20, 30]` — 통과 시 고를 수 있는 이용 시간(분).
  차단 화면 버튼이 이 목록대로 만들어진다. 남은 한도보다 큰 값은 자동으로 빠진다
- `usagePenalty: 0.5` — 누적 사용량 보정
  ```
  ratio  = min(usedToday / dailyLimit, 1)
  factor = 1 - usagePenalty * ratio
  최종   = 기본확률 * factor
  ```
  한도를 다 쓰면 확률이 절반이 된다

### 검산표 (오늘 사용 0분 기준, 파이썬으로 확인함)

| 경과 | scurve | log | linear |
|---:|---:|---:|---:|
| 0분 | 0.0% | 0.0% | 0.0% |
| 10분 | 3.0% | 11.1% | 5.7% |
| 20분 | 7.5% | 19.7% | 11.3% |
| 30분 | 14.2% | 26.7% | 17.0% |
| 40분 | 23.3% | 32.6% | 22.7% |
| 60분 | 47.5% | 42.3% | 34.0% |
| 80분 | 71.8% | 50.0% | 45.3% |
| 120분 | 85.0% (상한) | 61.9% | 68.0% |
| 180분 | 85.0% (상한) | 74.9% | 85.0% (상한) |

scurve는 96분경 상한에 도달한다.
**scurve는 초반이 아주 느리다** (1분에 0.2%, 5분에 1.3%).
급하게 오르는 구간은 40~80분. 화면에서 숫자가 안 움직이는 것처럼 보여도 정상이다.

---

## 경과 시간 / 쿨타임 규칙

`waitStartAt`(확률 계산의 기준 시각)이 언제 갱신되는지가 핵심이다.

| 상황 | 기준 시각 |
|---|---|
| 설치 후 한 번도 안 씀 | **차단 화면이 처음 뜬 순간** (설치 시점이 아님) |
| 이용이 끝남 | 끝난 그 시각 → 0%부터 다시 |
| **거부당함** | **지금으로 초기화** → 0%부터 다시 |

- `cooldownSec: 60` — 거부되면 60초간 버튼이 잠긴다
- **거부 시 경과 시간까지 초기화되는 건 의도된 것이다.**
  80분 기다려 71.8%를 만들었다가 실패하면 다시 80분을 기다려야 한다.
  한 번의 판정을 무겁게 만들려는 설계.
- 그 결과 **쿨타임은 사실상 무의미하다.** 초기화 직후 확률이 0%라
  60초 뒤에 눌러도 어차피 실패한다. 연타 방지용으로만 남아 있다.

---

## 상태 저장 (`chrome.storage.sync`)

`background.js` 맨 위의 `const STORE = chrome.storage.sync;` 한 곳에서 정한다.
말썽이 생기면 이 줄만 `chrome.storage.local`로 바꾸면 원래대로 돌아온다.

**`sync`를 쓰는 이유**: 같은 구글 계정으로 로그인한 프로필끼리 값이 공유된다.
프로필 A에서 한도를 다 쓰면 프로필 B에서도 0분 남음이 된다.
프로필을 바꿔 한도를 초기화하는 우회를 막으려는 것. (완전하지는 않다 — 아래 참고)

**주의**: `sync`는 쓰기 횟수 제한이 있다 (분당 120회, 시간당 1800회).
`saveState()`는 판정할 때와 세션 시작·종료할 때만 부른다.
차단 화면이 1초마다 하는 건 **읽기뿐**이라 제한에 걸리지 않는다.
여기에 쓰기를 추가하면 바로 한도를 넘기니 조심할 것.

값은 5개뿐. `background.js`의 `DEFAULT_STATE` 참고.

| 키 | 뜻 |
|---|---|
| `waitStartAt` | 확률 계산의 기준 시각(ms). null이면 아직 안 정해짐 |
| `todayKey` | `'2026-09-10'` 형식. 날이 바뀌면 사용량 자동 초기화 |
| `usedTodayMin` | 오늘 이미 쓴 시간(분) |
| `sessionEndAt` | 이용 중이면 끝나는 시각(ms), 아니면 null |
| `cooldownUntil` | 언제까지 시도 못 하는지(ms) |

**사용량은 세션이 끝날 때가 아니라 시작할 때 미리 차감한다.**
끝날 때 빼면 20분을 받고 5분 만에 브라우저를 꺼버렸을 때 기록이 안 남는다.

---

## 메시지 프로토콜

`blocked.js`와 `timer.js`가 `chrome.runtime.sendMessage`로 background에 요청한다.
`background.js`의 `handleMessage`가 전부 처리한다.

| 요청 | 보내는 쪽 | 응답 |
|---|---|---|
| `GET_STATUS` | blocked.js (1초마다) | `buildStatus()` 결과 전체 |
| `ATTEMPT` | blocked.js | `{ passed, roll, prob, status }` |
| `START_SESSION` `{minutes}` | blocked.js | `{ ok, endAt }` |
| `GET_SESSION` | timer.js | `{ sessionEndAt }` |
| `DEV_SET_ELAPSED` `{minutes}` | blocked.js (테스트) | 갱신된 status |
| `DEV_SET_USED` `{minutes}` | blocked.js (테스트) | 갱신된 status |

`buildStatus()`가 돌려주는 것: `elapsedMin`, `usedTodayMin`, `remainingMin`,
`cooldownLeftSec`, `dailyLimit`, `prob`, `durationOptions`, `canAttempt`

> `ATTEMPT`가 쿨타임/한도에 막히면 `{ passed:false, blocked:true, status }`를
> 돌려준다. 이때는 **`roll`과 `prob`이 없다.** 받는 쪽에서 `outcome.blocked`를
> 먼저 확인하지 않으면 `undefined.toFixed()`로 터진다.

---

## 차단 해제 메커니즘

`rules.json`은 **건드리지 않는다.** 대신 이용 중에만 동적 규칙을 위에 덮어씌운다.

| | id | priority | action |
|---|---|---|---|
| 정적 (`rules.json`) | 1 | 1 | redirect → blocked.html |
| 동적 (`background.js`) | 1000 | **2** | allow |

priority가 높은 쪽이 이기므로, allow 규칙이 있는 동안만 유튜브가 열린다.
세션이 끝나면 이 규칙만 제거하면 원래대로 차단된다.

`setAllowRule()`은 항상 `removeRuleIds: [ALLOW_RULE_ID]`를 먼저 넣는다.
같은 id의 규칙이 이미 있으면 오류가 나기 때문이다.

---

## 크롬 MV3 함정 (전부 실제로 부딪힌 것)

1. **서비스 워커는 몇 초 만에 잠든다.**
   기억할 값을 일반 변수에 두면 전부 날아간다. 반드시 `chrome.storage`.

2. **`setTimeout`으로 긴 타이머를 재면 안 된다.**
   20분을 재는 도중에 워커가 죽어서 타이머가 사라진다.
   → `chrome.alarms`를 쓴다. 꺼져 있어도 시간이 되면 깨워준다.

3. **차단 규칙은 "새로 이동할 때"만 작동한다.**
   시간이 끝나도 이미 열려 있는 탭은 그대로 재생된다.
   → `reloadYoutubeTabs()`로 직접 새로고침해서 차단 화면으로 돌려보낸다.

4. **브라우저 재시작 시 알람은 날아가는데 동적 규칙은 남는다.**
   그대로 두면 차단이 영영 안 걸린다.
   → `reconcile()`이 `onStartup`/`onInstalled`에서 상태를 맞춘다.

5. **`onMessage`에서 `return true`를 빼먹으면 답장이 안 간다.**
   비동기로 답하겠다는 표시다.

6. **화면은 스스로 갱신되지 않는다.**
   한 번 그리고 끝내면 시간이 흘러도 숫자가 멈춰 있다. (실제로 겪은 버그)
   → `blocked.js`가 1초마다 `GET_STATUS`를 다시 물어본다.

7. **`chrome.tabs.query`는 `"tabs"` 권한 없이 `host_permissions`로 동작 중이다.**
   `<all_urls>`를 좁히면 `reloadYoutubeTabs()`가 조용히 깨진다.

---

## 알려진 문제 / 기술 부채

- **차단 대상 도메인이 3곳에 흩어져 있다.** 유튜브 외 사이트를 추가하려면
  `rules.json`, `background.js`의 `setAllowRule()` urlFilter,
  `manifest.json`의 `content_scripts.matches`를 **동시에** 고쳐야 한다.
  숫자는 `config.js`에 모았지만 도메인은 아직 못 모았다.
- **프로필·계정 전환으로 우회할 수 있다.** 확장은 자기가 설치된 프로필
  안에서만 동작하므로 코드로는 막을 수 없는 **구조적 한계**다.
  - **압축해제 확장은 구글 계정을 따라 동기화되지 않는다.** 그래서 새 프로필을
    만들면 확장 자체가 없고, 유튜브가 그냥 열린다. (웹스토어 확장만 동기화됨)
  - `storage.sync`가 실제로 커버하는 건 **"같은 구글 계정 + 각 프로필에 확장을
    수동 설치"** 한 경우뿐이다. 그 외에는 도움이 안 된다.
  - **다른 구글 계정 간 한도 공유는 `sync`로 불가능하다.** 계정마다 저장 공간이
    완전히 분리돼 있다. → **6차시 Supabase에서 "공유 코드" 방식으로 해결 예정**
    (각 프로필에 같은 코드를 입력 → 계정과 무관하게 한도 공유).
    `loadState`/`saveState` 두 함수만 갈아끼우면 되도록 `STORE`로 모아둔 상태
  - 그 밖에 시크릿 모드, 게스트 모드, `chrome://extensions`에서 토글 끄기로도
    뚫린다. 윈도우 레지스트리 크롬 정책으로 막을 수 있으나 **하지 않기로 함**
    (너무 강한 통제라 프로젝트 취지에 안 맞음)
  → **보고서에는 "완전 차단이 아니라 우회에 마찰을 주는 것이 목표"로 쓸 것.**
    한계를 실험으로 확인했다는 내용 자체가 좋은 소재다.
- **유튜브는 SPA라 내부 이동이 `main_frame`으로 안 잡힌다.**
  지금 구조에서는 큰 문제가 아니지만(차단 상태면 애초에 못 들어감),
  나중에 다른 사이트를 추가할 때 다시 볼 것.
- **종료 1분 전 알림 미구현.** `timer.js`가 마지막 1분에 빨개지는 것까지만 됨.

---

## 하지 말 것

- `manifest.json`과 `rules.json`은 **함부로 고치지 말고 먼저 물어볼 것.**
  겨우 작동하는 상태라 깨지면 곤란하다.
- 숫자를 `config.js` 밖에 흩뿌리지 말 것.
- `background.js`에서 값을 일반 변수에 담아두지 말 것 (워커가 잠들면 사라진다).
- `blocked.js`나 `timer.js`에서 확률을 직접 계산하지 말 것.
  판정의 주인은 `background.js` 하나여야 한다.
- 화면 꾸미기를 지금 하지 말 것. 나중에 한 번에 할 예정.

---

## 작업 로그

기록 형식: `### YYYY-MM-DD [태그]` — 작업 내용 / 결정 사항 / TODO

### 1차시 [Claude Code]
- **작업**: 저장소 생성. `manifest.json`, `rules.json`, `blocked.html`로
  최소 차단 동작 확인
- **결정**: `declarativeNetRequest` 정적 규칙 + `web_accessible_resources`로
  차단 페이지를 여는 구조

### 2026-09-07 (2차시) [Claude Code]
- **작업**: 확률 로직과 판정 흐름 전체 연결
  - `config.js` / `probability.js` 작성. 세 개형 수치를 파이썬으로 검산
  - `background.js` 작성 — 판정 → 차단 해제 → 시간 종료 → 재차단 한 바퀴
  - `blocked.js` 작성, `blocked.html` 개편 (동작 확인용 최소 화면)
  - `timer.js` 추가 — 유튜브 위 남은 시간 표시
- **결정 사항**
  - `probability.js`는 순수 계산만. 저장·화면·난수 전부 background로
  - 전역 스크립트 방식 채택 (ES 모듈 아님). `importScripts` 때문
  - scurve에서 `s(0)`을 빼고 정규화 — 0분에 정확히 0%가 나오게
  - 사용량은 세션 **시작할 때** 미리 차감
  - `rules.json` 대신 priority 2 동적 allow 규칙으로 차단 해제
  - **거부되면 경과 시간도 초기화** (처음엔 유지하는 쪽이었으나 뒤집음)
- **실험 결과**: scurve `midpoint 60`/`steepness 0.05`에서
  30분 14.2% / 60분 47.5% / 80분 71.8%. 실제 브라우저에서 한 바퀴 도는 것 확인
- **버그 수정**: 차단 화면이 열릴 때 한 번만 확률을 받아와서 시간이 흘러도
  숫자가 멈춰 있던 문제 → 1초마다 `GET_STATUS` 재요청으로 해결
- **TODO**: 화면 꾸미기 / 종료 1분 전 알림 / 도메인 설정 일원화

### 2026-09-10 [Claude Code]
- **작업**: 코드 전체를 다시 읽고 CLAUDE.md를 실제 구현 기준으로 재작성.
  메시지 프로토콜 표, MV3 함정 7가지, 기술 부채 목록을 새로 정리
- **TODO**: 아래 "다음에 할 것" 참고

### 2026-09-12 [Claude Code]
- **작업**: 기술 부채 3건 처리
  - `_metadata/`를 `.gitignore`에 넣고 `git rm -r --cached`로 저장소에서 제거
  - `config.js`에 `devMode` 추가. 테스트 칸을 지우는 대신 스위치로 전환.
    화면에서 숨기는 것만이 아니라 `background.js`에서 `DEV_*` 요청도 막는다
    (콘솔에서 직접 보내 한도를 늘리지 못하게)
  - `blocked.html`이 `config.js`를 불러오도록 추가 (`CONFIG`를 쓰기 위해)
  - 저장소를 `chrome.storage.local` → **`chrome.storage.sync`** 로 전환.
    `background.js` 맨 위 `const STORE` 한 줄로 모아둠
- **결정 사항**
  - 계정/프로필 전환 우회는 **확장으로 막을 수 없는 구조적 한계**로 규정.
    `sync`로 같은 계정 내 프로필만 커버하고, 나머지는 보고서에 한계로 명시
  - 테스트 코드는 삭제가 아니라 `devMode` 플래그로 관리 (되살리기 쉽게)
- **버그 수정**: `reconcile()`이 이용 중일 때 알람만 다시 걸고 통과 규칙은
  안 세우던 문제. 상태는 sync로 공유되지만 차단 해제 규칙은 프로필마다
  따로라, 다른 프로필이 "이용 중"인 줄만 알고 유튜브는 막혀 있었다
  → `setAllowRule(true)`를 함께 호출하도록 수정
- **주의**: sync로 바꾸면서 기존 `storage.local`에 있던 값은 안 넘어간다.
  한도·경과 시간이 한 번 초기화된 상태로 시작한다
- **TODO**: 아래 "다음에 할 것" 참고

### 2026-09-13 [Claude Code]
- **작업**
  - 이용 시간 선택지 `[5, 10, 20, 30]`이 `background.js`에 박혀 있던 것을
    `config.js`의 `durationOptions`로 옮김 ("숫자는 config에만" 규칙 위반이었음)
  - 계정·프로필 전환 우회의 실제 범위를 조사해 "알려진 문제"에 정리
- **알게 된 것 (중요)**: **압축해제 확장은 구글 계정을 따라 동기화되지 않는다.**
  그래서 새 프로필에는 확장 자체가 없고, `storage.sync`로 바꾼 효과는
  "같은 계정 + 각 프로필에 수동 설치"한 경우로 한정된다.
  다른 구글 계정 간 한도 공유는 `sync`로는 원천적으로 불가능
- **결정 사항**
  - 윈도우 레지스트리 크롬 정책은 **쓰지 않기로 함.** 통제가 너무 강해
    프로젝트 취지에 안 맞는다
  - 다른 계정 간 한도 공유는 **6차시 Supabase에서 "공유 코드" 방식**으로.
    지금 미리 만들지 않는다
  - 당장은 각 프로필에 확장을 수동 설치해 **"차단은 걸리되 한도는 프로필별로
    따로"** 상태로 둔다
- **TODO**: 아래 "다음에 할 것" 참고

---

## 다음에 할 것

1. **화면 꾸미기** — 지금은 동작 확인용 최소 상태
2. **종료 1분 전 알림** — `timer.js`가 빨개지는 것까지만 되어 있음
3. **도메인 설정 일원화** — 3곳에 흩어진 차단 대상을 한 곳으로.
   1단계는 `setAllowRule()`만 `config.js`를 읽게 (안전, 5분).
   2단계는 `rules.json`을 동적 규칙으로 옮기고 `content_scripts`를
   `chrome.scripting`으로 대체 — "사이트 추가" 기능 만들 때 같이
4. **차단할 사이트 추가 / 설정 화면**
5. **기록 저장과 대시보드** (6차시부터 Supabase)
   - 여기서 **"공유 코드"** 도 같이. 각 프로필에 같은 코드를 입력하면
     구글 계정과 무관하게 한도가 공유된다.
     `loadState`/`saveState`만 Supabase를 보게 바꾸면 나머지는 손댈 것 없음
6. 제출 전 `config.js`의 `devMode`를 `false`로

> 크롬 정책 레지스트리는 **하지 않기로 결정**했다. 다시 꺼내지 말 것.
