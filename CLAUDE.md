# 확률 기반 사이트 차단 확장 프로그램

## 프로젝트 개요
고등학교 정보과학 수업 프로젝트 (8차시).
차단된 사이트에 접속을 시도하면 확률로 통과 여부를 판정하고,
통과 시 하루 한도 안에서 고른 시간만큼만 접속을 허용한다.

## 만드는 것
1. 크롬 확장 프로그램 (메인) — 차단, 판정, 타이머
2. 웹 대시보드 — 기록 통계, Vercel 배포, Supabase 연동

## 핵심 로직
- 마지막 이용 종료 시각부터 경과 시간이 길수록 통과 확률 상승
- 확률에는 상한이 있어 무한정 오르지 않음
- 그날 누적 사용 시간이 많을수록 확률 상승 속도가 느려짐
- 최종 확률로 난수를 뽑아 통과/거부 판정
- 통과 시 5/10/20/30분 중 선택, 하루 한도에서 차감
- 거부 시 쿨타임 동안 재시도 불가
- 남은 한도가 0이면 판정 없이 차단
- 확률 관련 수치는 전부 config 한 곳에 모아둘 것

## 확률 수식과 초기값
전부 `config.js`에 있다. 다른 파일에는 숫자를 쓰지 않는다.
아래는 조정하면서 쓰는 시작값이고, 확정된 값이 아니다.

기본 개형은 **scurve**(로지스틱). 세 가지 중 `curve` 값으로 전환한다.

- `scurve` — `midpoint: 60`, `steepness: 0.05`
  - `s(t) = 1 / (1 + exp(-steepness * (t - midpoint)))`
  - 그냥 쓰면 0분에서도 4.7%가 나오므로 `s(0)`을 빼고 정규화한다
  - `p = 100 * (s(t) - s(0)) / (1 - s(0))`
  - 결과: 30분 14.2% / 60분 47.5% / 80분 71.8% / 96분경 상한 도달
- `log` — `p = A * ln(1 + t / smoothness)`, `smoothness: 30`
  - A는 고정값이 아니라 "`anchorMinutes`(80분)에 `anchorProb`(50%)"에서 역산
- `linear` — `p = maxProb * (t / linearFullMin)`, `linearFullMin: 150`

공통 보정:
- `maxProb: 85` — 상한. 세 개형 모두 여기서 자른다
- `dailyLimit: 60` — 하루 총 이용 한도(분)
- `usagePenalty: 0.5` — 누적 사용량 보정
  - `ratio = min(usedToday / dailyLimit, 1)`
  - `factor = 1 - usagePenalty * ratio`
  - 한도를 다 쓰면 확률이 절반이 된다

## 쿨타임 / 경과 시간 규칙
- `cooldownSec: 60` — 거부되면 60초 동안 시도 버튼이 잠긴다
- **거부되면 경과 시간도 0으로 초기화된다.** 확률이 0%부터 다시 자라므로
  한 번 실패하면 처음부터 다시 기다려야 한다
- 이용이 끝나면 그 시각이 새 기준이 된다 (0%부터 시작)
- 설치 후 한 번도 안 썼다면, **차단 화면이 처음 뜬 순간**이 기준이 된다
  (설치 시점이 아니다)

## 화면
- 차단 화면: 확률 크게 표시, 경과 시간·남은 한도, 시도 버튼
- 판정 결과: 통과는 초록, 거부는 빨강
- 이용 중: 우측 상단 플로팅 타이머, 종료 1분 전 알림
- 대시보드: 시도 횟수·통과율·사용 시간 카드, 최근 7일 그래프

## 파일 구조
| 파일 | 역할 |
|---|---|
| `manifest.json` | 확장 설정. 권한, background, content_scripts, 차단 규칙 경로 |
| `rules.json` | 정적 차단 규칙. youtube.com(main_frame)을 blocked.html로 리다이렉트 |
| `config.js` | 확률·시간 관련 모든 숫자. 조정은 여기서만 |
| `probability.js` | `calcProb(경과분, 오늘사용분)` → 확률(%). 계산만 하고 저장은 안 한다 |
| `background.js` | 서비스 워커. 판정, 차단 해제/재차단, 알람, chrome.storage 저장 |
| `blocked.html` | 차단 화면 뼈대 (꾸미기는 미뤄둔 상태) |
| `blocked.js` | 차단 화면 버튼 동작. 계산은 안 하고 background에 물어본다 |
| `timer.js` | content script. 유튜브 위에 남은 시간을 띄운다 |

저장하는 값 (`chrome.storage.local`) — 5개뿐:
`waitStartAt`(확률 기준 시각) / `todayKey`(날짜, 바뀌면 사용량 초기화) /
`usedTodayMin` / `sessionEndAt`(이용 중이면 끝나는 시각) / `cooldownUntil`

## 기술 제약
- Manifest V3
- declarativeNetRequest 사용. 이용 중에만 priority 2짜리 allow 동적 규칙을
  덮어씌워 차단을 푼다. `rules.json`은 건드리지 않는다
- 서비스 워커는 몇 초 만에 잠들기 때문에 기억할 값은 전부 storage에 넣는다
- 시간 종료는 `setTimeout`이 아니라 `chrome.alarms`를 써야 한다
- 유튜브 같은 SPA는 페이지 전환이 안 잡힐 수 있음
- 1~5차시 프론트, 6~8차시 Supabase

## 현재 진행 상황
- **1차시 완료** — 저장소 생성. manifest/rules/blocked.html로 최소 차단 동작 확인
- **2차시 완료 (2026-09-07)** — 확률 로직과 판정 흐름 전체 연결
  - `config.js` / `probability.js` 작성, 세 개형 수치 검산 완료
  - `background.js`로 판정 → 차단 해제 → 시간 종료 → 재차단 한 바퀴 연결
  - `timer.js`로 유튜브 위 남은 시간 표시
  - 차단 화면에 테스트용 칸(경과·사용량 강제 설정)을 넣어둔 상태
  - 실제 브라우저에서 한 바퀴 도는 것 확인함

### 다음에 할 것
- 화면 꾸미기 (지금은 동작 확인용 최소 상태로 미뤄둠)
- 종료 1분 전 알림 (`timer.js`가 마지막 1분에 빨개지는 것까지만 되어 있음)
- 차단할 사이트를 유튜브 외에 추가 / 설정 화면
- 기록 저장과 대시보드 (6차시부터 Supabase)

## 하지 말 것
- `manifest.json`과 `rules.json`은 함부로 고치지 말고 먼저 물어볼 것.
  겨우 작동하는 상태라 깨지면 곤란하다
- 숫자를 `config.js` 밖에 흩뿌리지 말 것
- `background.js`에서 값을 일반 변수에 담아두지 말 것. 서비스 워커가
  잠들면 사라진다. 반드시 `chrome.storage`를 쓸 것

## 작업 방식 (중요)
- 나는 웹 개발 초보다. 코드를 한 번에 다 만들지 말고
  작은 단위로 나눠서 진행하고, 각 코드가 무슨 역할인지 설명해줘.
- 내가 이해하고 직접 수정할 수 있는 수준을 목표로 한다.
- 파일을 새로 만들거나 크게 고치기 전에 먼저 물어봐줘 + 설명해줘.
